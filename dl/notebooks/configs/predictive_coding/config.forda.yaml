data:
  dataset_name: FordA
  data_dir: data/predictive_coding

dataloader:
  batch_size: 128
  drop_last: false
  num_workers: 0
  val_split: 0.1

encoder:
  hidden_size: 128
  norm_mode: layerNorm
  conv_layers:
    - {kernel_size: 10, stride: 5, padding: 3}
    - {kernel_size: 8,  stride: 4, padding: 2}

ar:
  mode: GRU
  hidden_size: 256
  n_levels: 1
  reverse: true

loss:
  n_predictions: 6
  negative_sampling_ext: 256
  temperature: 0.07

optimizer:
  learning_rate: 0.0002
  weight_decay: 0.000001

trainer:
  epochs: 250
  output_dir: lightning_logs/forda_cpc

runtime:
  seed: 137
  accelerator: auto
  devices: 1
